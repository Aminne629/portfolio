name: Mise à jour Portfolio
on:
  push:
    branches:
      - main  # Vérifie bien que ta branche principale s'appelle "main" (parfois c'est "master")

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Récupération du code
        uses: actions/checkout@v3

      - name: 📂 Envoi vers Alwaysdata via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /www/  # CHANGE CECI SI TON SITE EST DANS UN SOUS-DOSSIER